apiVersion: v1
kind: Service
metadata:
  name: api-svc
  labels:
    app: api
spec:
  type: LoadBalancer
  ports:
  - name: api
    protocol: TCP
    port: 80
    targetPort: 80
  selector:
    app: api
